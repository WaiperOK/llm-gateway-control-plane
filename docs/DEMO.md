# Demo Guide

1. Start local stack via Makefile (`make run` or `make docker-up`).
2. Execute one end-to-end request using `examples/` payloads.
3. Verify metrics endpoint and sample output artifacts.
4. Capture a short terminal recording for release notes.

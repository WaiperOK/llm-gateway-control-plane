# Release Policy

- `main` is the release branch.
- All changes must pass CI before tagging.
- Use semantic versioning (`MAJOR.MINOR.PATCH`).
- Each tag must include changelog updates and risk-impact notes.
